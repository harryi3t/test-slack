resources:
  - name: slack-service-repo
    type: gitRepo
    integration: git
    pointer:
      sourceName: harryi3t/slack-service

  - name: slack-test-repo
    type: gitRepo
    integration: git
    pointer:
      sourceName: harryi3t/test-slack

  - name: mesg-and-token
    type: params
    version:
      params:
        webhook: "Please remove this and add a secure variable from subs|proj settings page"
        message: "This is a sample message"
        channel: "slack-test"
